{
  "name": "japanese-kana-blocker",
  "version": "1.3.1",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "test": "node --test",
    "prepush": "npm test",
    "push": "jj git push --remote origin --bookmark main",
    "postpush": "npm run release",
    "release": "node scripts/release.mjs"
  }
}
